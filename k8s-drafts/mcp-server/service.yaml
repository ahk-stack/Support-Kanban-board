apiVersion: v1
kind: Service
metadata:
  name: support-kanban-mcp
  namespace: support-kanban
  labels:
    app: support-kanban-mcp
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 3100
      protocol: TCP
  selector:
    app: support-kanban-mcp
